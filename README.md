# Evergreen Consulting

Website for Andrea Schiralli, college admissions consultant. The site lives in `site/` (Astro, static output).

```bash
cd site && npm install && npm run dev
```

See `site/CLAUDE.md` for structure, design rules, copy voice and the SEO keyword map.
